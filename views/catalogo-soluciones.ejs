<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Catálogo de Soluciones</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            background: #f4f7f9;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            justify-content: center;
            padding: 40px;
        }

        .container {
            background: white;
            width: 900px;
            padding: 35px;
            border-radius: 14px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.12);
        }

        h2 {
            color: #1a365d;
            margin-bottom: 20px;
        }

        .solution {
            border: 1px solid #e5e7eb;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
        }

        .solution h4 {
            margin-bottom: 8px;
            color: #1f2937;
        }

        .solution p {
            font-size: 14px;
            color: #475569;
            margin-bottom: 6px;
        }

        .tag {
            display: inline-block;
            background: #e0f2fe;
            color: #0369a1;
            padding: 4px 10px;
            font-size: 12px;
            border-radius: 999px;
            font-weight: 600;
        }
    </style>
</head>

<body>

<div class="container">
    <h2>Catálogo de Soluciones</h2>

    <% soluciones.forEach(s => { %>
        <div class="solution">
            <span class="tag"><%= s.categoria %></span>
            <h4><%= s.problema %></h4>
            <p><strong>Posible causa:</strong> <%= s.causa %></p>
            <p><strong>Solución:</strong> <%= s.solucion %></p>
        </div>
    <% }) %>
</div>

</body>
</html>
